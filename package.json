{
  "name": "interactive-comments-section-main",
  "version": "1.0.0",
  "description": "Interactive comments section",
  "main": "index.js",
  "homepage": "https://lArkl.github.io/fm-interactive-comments-section",
  "scripts": {
    "format": "prettier \"./src/**/*.{js,html}\" --write",
    "start": "parcel index.html --open",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.html --public-url .",
    "predeploy": "rm -rf dist && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "lArkl",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "parcel-bundler": "^1.12.5",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
